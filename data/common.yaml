---
secrets::defaults:
  owner:           root
  group:           root
  mode:           '0400'
  mandatory:       true
  notify_services: NULL
  posix_acl:       NULL
  selrange:        NULL
  seltype:         NULL
  seluser:         NULL
  secretbase: /etc/puppet/secrets
  selinux_ignore_defaults: NULL
